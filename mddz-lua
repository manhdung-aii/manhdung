-- [[ NMD HUB - PHIÊN BẢN FULL TÍNH NĂNG ]] --
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
-- [[ NMD HUB - SMART TIMER UPDATE ]] --
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- [[ BIẾN CẤU HÌNH ]] --
_G.AntiAdmin = true
_G.AutoLeave30Min = false -- Mặc định tắt theo ý bạn
local StartTime = tick()

-- [[ HỆ THỐNG TỰ THOÁT SAU 30 PHÚT ]] --
task.spawn(function()
    while task.wait(10) do -- Kiểm tra mỗi 10 giây để tiết kiệm tài nguyên
        if _G.AutoLeave30Min then
            local ElapsedTime = tick() - StartTime
            local RemainingTime = 1800 - ElapsedTime -- 30 phút = 1800 giây
            
            -- Thông báo khi còn 1 phút (60 giây)
            if RemainingTime <= 60 and RemainingTime > 50 then
                Fluent:Notify({
                    Title = "NMD HUB TIMER",
                    Content = "Cảnh báo: Sẽ tự động thoát Server sau 60 giây nữa!",
                    Duration = 10
                })
            end
            
            -- Thực hiện thoát khi đủ 30 phút
            if ElapsedTime >= 1800 then
                game.Players.LocalPlayer:Kick("\n[NMD HUB TIMER]\nĐã đủ 30 phút treo máy!\nScript tự động thoát theo cài đặt của bạn.")
            end
        else
            -- Reset lại thời gian nếu người dùng tắt đi rồi bật lại
            StartTime = tick()
        end
    end
end)

-- [[ GIAO DIỆN ]] --
local Window = Fluent:CreateWindow({
    Title = "NMD HUB",
    SubTitle = "Smart Farming Edition",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = false,
    Theme = "Dark"
})

local Tabs = {
    Main = Window:AddTab({ Title = "Auto Farm", Icon = "home" }),
    Settings = Window:AddTab({ Title = "Cài Đặt", Icon = "settings" })
}

-- [[ TAB SETTINGS: CẬP NHẬT TÍNH NĂNG MỚI ]] --
Tabs.Settings:AddSection("Security & Timer")

-- Nút tự thoát sau 30 phút
Tabs.Settings:AddToggle("AutoLeaveToggle", {
    Title = "Auto Leave (30 Minutes)", 
    Description = "Tự động thoát game sau khi treo đủ 30 phút.",
    Default = false 
}):OnChanged(function(v) 
    _G.AutoLeave30Min = v 
    if v then
        StartTime = tick() -- Reset mốc thời gian khi vừa bật nút
        Fluent:Notify({
            Title = "NMD HUB",
            Content = "Đã bật đếm ngược 30 phút thoát Server!",
            Duration = 5
        })
    end
end)

-- Nút Anti-Admin (Mặc định bật như bản trước)
Tabs.Settings:AddToggle("AntiAdmin", {
    Title = "Anti-Admin (Auto Kick)", 
    Default = true 
}):OnChanged(function(v) _G.AntiAdmin = v end)

-- Ngôn ngữ
Tabs.Settings:AddDropdown("Lang", {
    Title = "Language",
    Values = {"Tiếng Việt", "English"},
    Default = "Tiếng Việt"
})

-- [ GIỮ NGUYÊN LOGIC FARM VÀ CHEST Ở ĐÂY ] --

Window:SelectTab(1)
Fluent:Notify({
    Title = "NMD HUB",
    Content = "Đã cập nhật tính năng Hẹn giờ 30 phút trong Cài Đặt!",
    Duration = 5
})
local Window = Fluent:CreateWindow({
    Title = "NMD HUB",
    SubTitle = "Chơi game vui vẻ nha!",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- [[ CẤU HÌNH ]] --
_G.AutoFarm = false
_G.AutoStats = false
_G.FastAttack = true
_G.FarmDistance = 15
_G.StatPoint = "Melee" -- Mặc định nâng Melee

-- [[ LOGO CHỮ D XANH (FIXED) ]] --
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Logo = Instance.new("ImageButton", ScreenGui)
Logo.Size = UDim2.new(0, 55, 0, 55)
Logo.Position = UDim2.new(0.1, 0, 0.15, 0)
Logo.Image = "rbxassetid://14457319666"
Logo.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Logo.BackgroundTransparency = 0.4
Logo.Draggable = true
Logo.Active = true
Instance.new("UICorner", Logo).CornerRadius = UDim.new(0, 12)
local Stroke = Instance.new("UIStroke", Logo)
Stroke.Color = Color3.fromRGB(0, 255, 255)
Stroke.Thickness = 2
Logo.MouseButton1Click:Connect(function()
    if Window.Root then Window.Root.Visible = not Window.Root.Visible end
end)

-- [[ TẠO CÁC TAB ]] --
local Tabs = {
    Main = Window:AddTab({ Title = "Auto Farm", Icon = "home" }),
    Stats = Window:AddTab({ Title = "Nâng Điểm", Icon = "bar-chart" }),
    Settings = Window:AddTab({ Title = "Cài Đặt", Icon = "settings" })
}

-- [[ TAB CHÍNH ]] --
Tabs.Main:AddToggle("FarmLvl", {Title = "Auto Farm Level", Default = false }):OnChanged(function(v) _G.AutoFarm = v end)
Tabs.Main:AddToggle("FastAtk", {Title = "Fast Attack (Đánh Nhanh)", Default = true }):OnChanged(function(v) _G.FastAttack = v end)

-- [[ TAB NÂNG ĐIỂM ]] --
Tabs.Stats:AddDropdown("SelectStat", {
    Title = "Chọn chỉ số nâng",
    Values = {"Melee", "Defense", "Sword", "Gun", "Demon Fruit"},
    Default = "Melee",
    Callback = function(v) _G.StatPoint = v end
})
Tabs.Stats:AddToggle("AutoStat", {Title = "Tự động nâng điểm", Default = false }):OnChanged(function(v) _G.AutoStats = v end)

-- [[ TAB CÀI ĐẶT ]] --
Tabs.Settings:AddSection("Nhà Sản Xuất: NMD")
Tabs.Settings:AddParagraph({Title = "NMD HUB", Content = "Chơi game vui vẻ nha!"})
Tabs.Settings:AddSlider("Distance", {
    Title = "Khoảng cách Farm",
    Default = 15, Min = 1, Max = 36, Rounding = 0,
    Callback = function(v) _G.FarmDistance = v end
})

-- [[ LOGIC AUTO FARM LEVEL ]] --
task.spawn(function()
    while task.wait() do
        if _G.AutoFarm then
            pcall(function()
                local LP = game.Players.LocalPlayer
                -- Tự động cầm vũ khí
                local Tool = LP.Backpack:FindFirstChild("Melee") or LP.Character:FindFirstChild("Melee")
                if Tool and not LP.Character:FindFirstChild(Tool.Name) then
                    LP.Character.Humanoid:EquipTool(Tool)
                end
                
                -- Tìm quái (Logic đơn giản hóa cho Delta)
                local Enemy = nil
                for _, v in pairs(workspace.Enemies:GetChildren()) do
                    if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                        Enemy = v break
                    end
                end
                
                if Enemy then
                    -- Bay đến quái
                    LP.Character.HumanoidRootPart.CFrame = Enemy.HumanoidRootPart.CFrame * CFrame.new(0, _G.FarmDistance, 0)
                    -- Fast Attack
                    if _G.FastAttack then
                        local Combat = require(LP.PlayerScripts.CombatFramework)
                        local Lib = getupvalue(Combat, 2)
                        if Lib and Lib.activeController then
                            Lib.activeController:attack()
                        end
                    end
                end
            end)
        end
    end
end)

-- [[ LOGIC AUTO STATS ]] --
task.spawn(function()
    while task.wait(1) do
        if _G.AutoStats then
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("AddPoint", _G.StatPoint, 3)
        end
    end
end)

Window:SelectTab(1)
Fluent:Notify({Title = "NMD HUB", Content = "Đã cập nhật Full tính năng!", Duration = 5})
